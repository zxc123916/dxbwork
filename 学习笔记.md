# 工业机器人健康状态监测 — 学习笔记
> **课程名称：** 制造智能技术
> **选题方向：** 工业机械臂抓手故障检测
> **核心技术：** CNN‑BiLSTM深度学习、时序特征工程、B/S系统开发
> **日期：** 2026‑08‑24

## 一、Git与GitHub仓库管理
课程设计使用Git完成版本管理，保存完整提交记录。
1. 本地配置git用户名与邮箱，设置主分支为main。
2. GitHub创建私有仓库，配置`.gitignore`忽略缓存、虚拟环境、模型权重。
3. 提交格式遵循：`feat/fix/docs(模块): 简短描述`，小步提交，保留开发过程。
> 常用命令：`git add .`、`git commit -m "描述"`、`git push origin main`。

## 二、选题调研
选题：**工业机械臂抓手丢失故障检测系统**，面向工业设备运维。
- 数据集：机器人传感器时序数据，故障样本少，样本不平衡。
- 用到技术点：
  1. 智能故障诊断：CNN‑BiLSTM实现时序故障识别
  2. 工业数据管理：SQLite存储原始数据、推理结果
  3. 人机交互：FastAPI后端 + HTML前端搭建B/S演示系统

## 三、AI辅助开发实践
按照任务要求使用AI辅助开发，留存prompt会话记录。
1. 任务拆解：数据预处理 → 模型训练 → 推理封装 → 后端接口 → 前端页面 → 联调测试。
2. 开发流程：AI生成代码，人工审核调试，针对报错定位修复。
3. AI辅助产出：EDA预处理脚本、CNN‑BiLSTM训练代码、FastAPI后端、HTML前端页面。
4. 报告需要记录AI报错以及人工修改的过程。

## 四、算法模块实现
1. **数据处理**：EDA分析、异常值截断、标准化，滑动窗口构造时序样本。
2. **模型方案**：CNN‑BiLSTM混合网络；Focal Loss处理样本不均衡，使用MixUp、Dropout、早停策略降低过拟合。
3. 说明：受故障样本数量限制，模型指标存在上限，课程设计重点完成整套可运行演示系统。
4. 导出训练权重，封装`model_infer.py`推理模块，供后端调用。

## 五、B/S系统架构实现
轻量B/S架构，不引入重型前端框架。
### 项目目录