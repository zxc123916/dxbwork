# 方案设计

## 1 功能需求分析
### 1.1 功能性需求
本系统面向工业机器人智能故障监测场景，基于深度学习与时序数据分析技术，实现机器人运行状态自动识别，主要功能需求如下：

1. **数据解析功能**
支持读取机器人多传感器时序数据，完成数据清洗、标准化、时序窗口重构，自动生成模型可识别的输入样本。

2. **智能故障识别功能**
依托 CNN-LSTM 混合深度学习模型，对机器人运行数据进行特征提取与时序推理，实现正常状态、故障状态二分类，并输出故障概率值。

3. **Web 在线推理功能**
后端提供可视化接口，支持前端提交数据、后台调用模型推理、实时返回诊断结果，实现完整 B/S 交互流程。

4. **结果可视化展示功能**
前端展示故障标签、故障概率及健康状态文字评价，区分正常/故障视觉样式，界面简洁直观、可直接演示。

### 1.2 非功能性需求
1. **稳定性**：服务启动稳定，推理流程完整，无程序崩溃、卡死问题。
2. **易用性**：页面操作简单，一键检测，无需复杂配置。
3. **可扩展性**：模型、接口、页面完全解耦，可扩展多分类故障、历史记录、数据可视化曲线等功能。
4. **可演示性**：适配课程设计答辩演示场景，效果直观、运行流畅。

## 2 方案论证
### 2.1 时序建模方案论证
机器人传感器数据属于**高维、连续、强时序、噪声多**数据。

1. **纯 CNN 方案**：擅长局部特征提取，但无法捕捉前后时序依赖，故障识别精度低。
2. **纯 LSTM 方案**：擅长时序拟合，但对局部突变噪声、局部故障特征提取能力弱。
3. **CNN+LSTM 混合方案**：
- CNN 负责滤波、局部特征压缩、降维去噪
- LSTM 负责学习长时序依赖关系
**结论：CNN-LSTM 混合模型最适配机器人故障诊断场景，精度最高、效果最好。**

### 2.2 系统架构方案论证
对比 C/S、B/S 架构：
- C/S 需要安装客户端，部署复杂、演示不便
- B/S 基于浏览器访问，轻量化、无需安装、适合课程展示

**最终采用 FastAPI + HTML 前后端分离轻量 B/S 架构。**

## 3 总体方案规划
### 3.1 技术栈方案
#### 后端技术
- Python3
- PyTorch（深度学习模型训练与推理）
- FastAPI（Web 后端服务）
- Pandas、Numpy（数据处理）
- Joblib（数据标准化模型保存）

#### 前端技术
- HTML5 + CSS3
- 卡片式现代化 UI、渐变背景、动态交互样式

#### 开发与管理工具
- VSCode
- Git / GitHub

### 3.2 外部资源配置方案
1. Python 环境依赖：通过 `requirements.txt` 统一管理
2. 模型资源：训练生成最优权重 `.pt` 文件、标准化 `.pkl` 文件
3. 无需数据库、无需第三方接口、完全本地独立运行

### 3.3 数据集来源方案
本系统采用**工业机器人公开传感器时序数据集**，包含机器人多维度运行传感数据，涵盖正常运行状态与多种故障运行状态。
数据集经过清洗、去噪、标准化、时序切片后生成 24 维特征时序样本，用于模型训练与系统测试。

### 3.4 前端 UI 方案
1. 深蓝色工业渐变背景，科技风系统界面
2. 白色悬浮卡片主体，阴影圆角提升质感
3. 渐变提交按钮、hover 动效、点击缩放
4. 智能结果区分：健康绿色提示、故障红色提示
5. 响应式布局

### 3.5 后端算法方案
1. **CNN 层**：一维卷积提取传感器局部特征、抑制噪声
2. **池化层**：降维压缩特征，减少参数量
3. **双向 LSTM 层**：前后向学习时序关联特征
4. **Dropout**：防止模型过拟合
5. **全连接层**：输出二分类概率，判断故障状态

## 4 技术路线
1. **数据准备阶段**：数据集读取、清洗、标准化、时序样本构建、数据集划分
2. **模型训练阶段**：搭建 CNN-LSTM 网络、训练调参、保存最优模型权重与标准化器
3. **模型推理封装阶段**：编写推理脚本、适配 Web 调用格式
4. **后端服务开发**：基于 FastAPI 搭建路由、接口、页面渲染逻辑
5. **前端页面开发**：美化界面、完善交互、结果动态展示
6. **系统联调测试**：整体跑通、排错优化、保证演示稳定
7. **文档整理与代码提交**：撰写设计文档、Git 提交推送仓库

## 5 项目进度计划
1. **第1阶段**：数据集处理、环境搭建、依赖配置
2. **第2阶段**：CNN-LSTM 模型搭建、训练、评估
3. **第3阶段**：模型推理代码封装、接口调试
4. **第4阶段**：前端页面美化、系统整体联调
5. **第5阶段**：项目优化、Bug修复、稳定演示
6. **第6阶段**：撰写设计文档、代码提交 GitHub、完成结题

## 6 参考文献
[1] 周志华。机器学习 [M]. 清华大学出版社，2016.
[2] 孙洁娣，毛新茹，温江涛，等。深度卷积长短期记忆网络的轴承故障诊断 [J]. 机械科学与技术，2021,40 (07):1091‑1099.
[3] 蒋沁诚，陶建峰，王洋洋，等。基于 EWT‑LSTM 的工业机器人关节异常检测 [J]. 浙江大学学报 (工学版),2025,59 (05):982‑994.
[4] 刘建伟，刘媛。长短期记忆网络 (LSTM) 研究进展 [J]. 控制与决策，2020,35 (06):1121‑1129.
[5] 韩谯，刘京，何国林，等。面向工业机器人关键部件的多源融合感知智能故障诊断方法研究 [J]. 机械工程学报，2025,61 (11):186‑198.
[6] 曹正志。改进 CNN‑LSTM 模型在滚动轴承故障诊断中的应用 [J]. 计算机系统应用，2021,30 (07):224‑229.
